machine:
  php:
    version: 7.1.0
dependencies:
  override:
    - cp .env.example .env
    - composer install --dev --prefer-source --no-interaction
    - chmod -R 777 storage
    - php artisan key:generate
deploy:
  production:
    branch: master
    heroku:
      appname: ci-demonstration
